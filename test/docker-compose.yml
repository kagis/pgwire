version: '3.4'
services:
  test:
    build: .
    environment:
      POSTGRES: postgres://postgres:5432
    working_dir: /opt/pgwire
    volumes:
      - ..:/opt/pgwire

  postgres:
    build: postgres